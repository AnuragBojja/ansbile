- name: conditions
  hosts: local
  connection: local
  vars_prompt:
    name: number
    prompt: Enter number
    private: false
  tasks:
  - name: check number is less than 10
    debug:
      msg: "given number {{ number }} is less than 10"
    when: number < 10